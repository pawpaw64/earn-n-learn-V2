
const express = require('express');
const router = express.Router();
const auth = require('../middleware/authMiddleware');

// TODO: Implement material controller and routes

module.exports = router;
